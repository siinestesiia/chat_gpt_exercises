# Chat GPT Exercises about functions

def find_max(lst):
    if len(numbers) == 0:
        return None
    max_num = lst[0]
    for item in lst:
        if item > max_num:
            max_num = item
    return max_num    

numbers = [3, 7, 2, 8, 4]

print(find_max(numbers))
